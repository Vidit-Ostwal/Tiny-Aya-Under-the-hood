# Tiny Aya Under The Hood

Investigating how Tiny Aya processes information across languages by analyzing how representations evolve across model layers. By comparing layer-wise representations in Tiny Aya Global and its regional variants, we identify where language-agnostic (universal) processing emerges and where region-specific specialization occurs.

## Setup

### Prerequisites

- Python 3.10+
- [uv](https://docs.astral.sh/uv/) (fast Python package manager)
- CUDA GPU recommended (Tiny Aya Global loads on 8GB VRAM at fp16)

### Installation

```bash
# Install uv (if not already installed)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Clone and enter the repo
git clone <repo-url>
cd Tiny-Aya-Under-the-hood

# Create virtual environment and install all dependencies
uv sync --extra all
```

### Environment variables

The FLORES+ dataset (our parallel corpus for cross-lingual analysis) is
gated on HuggingFace and requires authentication. Create a `.env` file
in the project root:

```bash
# .env
HF_TOKEN=hf_your_token_here
```

To get your token:
1. Create a HuggingFace account at https://huggingface.co/join
2. Accept the dataset terms at https://huggingface.co/datasets/openlanguagedata/flores_plus
3. Create an access token at https://huggingface.co/settings/tokens
4. Paste it into `.env` as shown above

The `python-dotenv` library loads this file automatically when any
analysis module needs the dataset. You can also add optional keys for
Kaggle, W&B, etc.

### Dependency groups

| Command | What it installs |
|---|---|
| `uv sync` | Core: torch, transformers, datasets, huggingface-hub, numpy, scipy, scikit-learn, matplotlib, seaborn, tqdm, accelerate, python-dotenv, pandas |
| `uv sync --extra dev` | Core + pytest, pytest-cov |
| `uv sync --extra notebooks` | Core + jupyter, ipywidgets |
| `uv sync --extra quantize` | Core + bitsandbytes (4-bit quantization) |
| `uv sync --extra all` | Everything above |

### Running tests

```bash
uv sync --extra dev
uv run pytest tests/ -v
```

### Running notebooks

```bash
uv sync --extra notebooks
uv run jupyter lab notebooks/
```

## Project Structure

```
.
├── pyproject.toml              # Project metadata and dependencies (uv/pip)
├── uth/                        # Main package
│   ├── __init__.py
│   ├── utils/
│   │   ├── __init__.py
│   │   └── languages.py        # 13-language registry with metadata
│   ├── data/
│   │   ├── flores_loader.py    # FLORES+ parallel corpus loader
│   │   └── translate_data_openai.py  # OpenAI translation pipeline
│   └── analysis/
│       ├── __init__.py
│       ├── cka.py              # CKA: linear, RBF, mini-batch, whitened
│       ├── hooks.py            # Activation extraction + model loading
│       ├── cross_lingual_alignment.py  # Main analyzer orchestrator
│       ├── retrieval_metrics.py  # MRR, Recall@k for translation retrieval
│       ├── clustering.py       # Hierarchical clustering, family dissolution
│       └── visualization.py    # Publication-quality plotting functions
├── notebooks/                  # Analysis notebooks (8 total)
│   ├── 01_data_preparation.py
│   ├── 02_activation_extraction.py
│   ├── 03_cross_lingual_cka.py
│   ├── 04_language_family_clustering.py
│   ├── 05_anisotropy_whitened_cka.py
│   ├── 06_retrieval_alignment.py
│   ├── 07_script_decomposition.py
│   └── 08_regional_comparison.py
├── tests/                      # Unit tests (71 tests)
│   ├── test_languages.py
│   ├── test_cka.py
│   ├── test_hooks.py
│   └── test_retrieval.py
└── results/                    # Output artifacts (generated by notebooks)
    └── cross_lingual/
        ├── activations/        # layer_{idx}_{lang}.pt
        ├── cka_matrices/       # layer_{idx}_{kernel}_cka.npy
        ├── metrics/            # convergence_curve.json, etc.
        └── figures/            # All generated plots
```

## Notebooks

| # | Notebook | Description |
|---|---|---|
| 01 | Data Preparation | Load FLORES+, corpus statistics, language metadata |
| 02 | Activation Extraction | Extract mean-pooled hidden states from Tiny Aya Global |
| 03 | Cross-Lingual CKA | Core CKA analysis, convergence curve, permutation tests |
| 04 | Language Family Clustering | Hierarchical clustering, family dissolution tracking |
| 05 | Anisotropy & Whitened CKA | Anisotropy measurement, whitened CKA comparison |
| 06 | Retrieval Alignment | Translation retrieval MRR, Recall@k per language |
| 07 | Script Decomposition | Intra- vs inter-script CKA, script bias analysis |
| 08 | Regional Comparison | Delta-CKA between Global and regional Tiny Aya models |

## Languages

13 languages across 5 families and 6 scripts:

| Language | ISO | Script | Family | Resource |
|---|---|---|---|---|
| English | en | Latin | Indo-European | High |
| Spanish | es | Latin | Indo-European | High |
| French | fr | Latin | Indo-European | High |
| German | de | Latin | Indo-European | High |
| Arabic | ar | Arabic | Afro-Asiatic | High |
| Hindi | hi | Devanagari | Indo-European | Mid |
| Bengali | bn | Bengali | Indo-European | Mid |
| Tamil | ta | Tamil | Dravidian | Mid |
| Turkish | tr | Latin | Turkic | Mid |
| Persian | fa | Arabic | Indo-European | Mid |
| Swahili | sw | Latin | Niger-Congo | Low |
| Amharic | am | Ge'ez | Afro-Asiatic | Low |
| Yoruba | yo | Latin | Niger-Congo | Low |
